
    /* API
     * ----------------------------------------------------------------------- */
    return {
        reorder: reorder,
        find_gene: find_gene,
        get_genes: get_genes,
        change_groups: change_groups
    };
}
